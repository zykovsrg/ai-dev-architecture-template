# Goal Log

Canonical hub-owned progress log for numeric goals in `ai/archiprojects.md`.
One line per progress event. Only `hub-goal-progress` writes here.

Columns: `дата` (YYYY-MM-DD), `goal_id` (a `kind: goal` id from the
archiproject registry), `сколько` (positive integer in that goal's `unit`),
`проект` (registered project id or `—`), `заметка` (free text or `—`).

| дата | goal_id | сколько | проект | заметка |
| --- | --- | --- | --- | --- |
