# Session Audit Ownership Transfer Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Move the reusable session-audit mechanism into `ai-dev-architecture` while preserving `hub-session-audit` as an unchanged archive.

**Architecture:** The architecture project owns the procedure, journal, generated safe metadata index, inventory script, test, and audit skill. The source project is read-only after this transfer. The scheduled task invokes the destination script only; selected transcript IDs remain the only authorization for content reading and immediate journal write.

**Tech Stack:** POSIX shell, Bash test, Markdown records, Codex recurring task.

## Global Constraints

- Do not copy, print, or persist transcript content during metadata refresh.
- The source project `/Users/zykovsrg/Documents/vibecode/_ai-hub/projects/hub-session-audit` is archive-only and must not be edited or deleted.
- Architecture/task/settings changes never happen automatically from audit findings.
- Protected architecture changes require `architecture-update` and a user confirmation after the exact replacement is shown.

---

### Task 1: Add the safe inventory script and its isolated test

**Files:**
- Create: `scripts/refresh-session-inventory.sh`
- Create: `scripts/test-refresh-session-inventory.sh`

**Interfaces:**
- Consumes: `SESSION_AUDIT_TRANSCRIPT_DIR`, `SESSION_AUDIT_OUTPUT_PATH`, and `SESSION_AUDIT_NOW` when set.
- Produces: `knowledge/session-audit/session-inventory.md` by default, with ID, bytes, and UTC modification time only.

- [ ] **Step 1: Write the failing test**

Create two empty `.jsonl` fixture files with known lengths and set the three environment variables. Assert the output includes:

```text
Всего сессий: 2
| `first-session` | 0 |
| `second-session` | 4 |
```

and assert it does not contain the fixture payload `private transcript text`.

- [ ] **Step 2: Run the test to verify it fails**

Run: `bash scripts/test-refresh-session-inventory.sh`

Expected: non-zero exit because `scripts/refresh-session-inventory.sh` is absent.

- [ ] **Step 3: Implement the minimal inventory script**

Implement a POSIX shell script that checks the transcript directory, gathers only filename, file size and mtime for direct `.jsonl` children, sorts the rows, and atomically replaces the Markdown output through a same-directory temporary file.

- [ ] **Step 4: Run the focused test**

Run: `bash scripts/test-refresh-session-inventory.sh`

Expected: `PASS: refresh-session-inventory`.

- [ ] **Step 5: Commit the runnable inventory component**

```bash
git add scripts/refresh-session-inventory.sh scripts/test-refresh-session-inventory.sh
git commit -m "feat: add architecture session inventory"
```

### Task 2: Move the procedure, journal, and safe index into architecture knowledge

**Files:**
- Create: `knowledge/session-audit/procedure.md`
- Create: `knowledge/session-audit/checked-sessions.md`
- Create: `knowledge/session-audit/session-inventory.md`

**Interfaces:**
- Consumes: the safe inventory generated by Task 1.
- Produces: one canonical procedure and journal under the architecture project.

- [ ] **Step 1: Copy only approved audit records**

Transfer the method’s nine checks, the no-secret evidence rule, the `0` checked count, and the current 51-session metadata index. Do not transfer transcript contents.

- [ ] **Step 2: Mark historical data precisely**

Keep the old 30-session queue only under an `Архивный снимок` heading. State that new runs use `session-inventory.md` and that the snapshot is not a selection queue.

- [ ] **Step 3: Validate record metadata and links**

Check each new record has valid frontmatter; update links so `procedure.md`, `checked-sessions.md`, and `session-inventory.md` form a closed set.

- [ ] **Step 4: Commit the knowledge transfer**

```bash
git add knowledge/session-audit
git commit -m "docs: move session audit records into architecture"
```

### Task 3: Add the protected audit workflow and replace the pilot-only policy

**Files:**
- Create: `ai/skills/session-audit/SKILL.md`
- Modify: `ai/architecture.md`
- Modify: `ai/decisions.md`
- Modify: `ai/current-task.md`
- Modify: `ai/future-tasks.md`

**Interfaces:**
- Consumes: the procedure and inventory from Task 2.
- Produces: a skill-loaded audit procedure and durable policy for the three-day separate task.

- [ ] **Step 1: Obtain the required protected-file confirmation**

Show the existing pilot-only wording and the proposed recurring workflow. Ask exactly: `Replace this?` Do not edit protected files until the user answers explicitly.

- [ ] **Step 2: Create the skill**

Write the English AI-facing `session-audit` skill: refresh metadata only; show candidates; require exact IDs; read only those IDs; immediately record safe results; propose, never apply, task/rule/settings changes; stop without writing if IDs are absent or invalid.

- [ ] **Step 3: Replace the architecture overview and decision**

Replace the proposal-only pilot policy with a compact overview: every three days starts a fresh task; only metadata refresh is automatic; exact selected IDs authorize content reading and journal write; all other changes need separate confirmation. Record the same invariant in `ai/decisions.md`.

- [ ] **Step 4: Align FT-20260826-003**

Change its goal, relevant files, risks, and done criteria from a small no-write pilot to the implemented semi-automatic mechanism. Mark the future-task entry as promoted if the project’s task workflow requires that state.

- [ ] **Step 5: Run consistency checks**

Run: `bash scripts/check-consistency.sh`

Expected: all canonical-list checks report `OK`.

- [ ] **Step 6: Commit the policy and task-memory updates**

```bash
git add ai/skills/session-audit/SKILL.md ai/architecture.md ai/decisions.md ai/current-task.md ai/future-tasks.md
git commit -m "feat: define recurring session audit workflow"
```

### Task 4: Point the recurring task to the architecture-owned script

**Files:**
- Modify: Codex recurring task `Самопроверка архитектуры — отдельный запуск`

**Interfaces:**
- Consumes: `scripts/refresh-session-inventory.sh` in this project.
- Produces: a fresh independent task every three days with no accumulated chat context.

- [ ] **Step 1: Preview the scheduled prompt**

Verify it uses the absolute destination script path and asks whether to show the metadata list after refresh. It must not read transcript contents itself.

- [ ] **Step 2: Update the recurring task**

Replace the old `hub-session-audit` script path with:

```text
/Users/zykovsrg/Documents/vibecode/_ai-hub/projects/ai-dev-architecture/scripts/refresh-session-inventory.sh
```

- [ ] **Step 3: Verify scheduling boundaries**

Confirm recurrence remains every three days, creates a separate task, and its prompt says that exact IDs are required before reading and journal write.

### Task 5: Verify the completed transfer

**Files:**
- Verify: all files introduced above.

- [ ] **Step 1: Run focused and repository tests**

Run:

```bash
bash scripts/test-refresh-session-inventory.sh
bash scripts/check-consistency.sh
git diff --check
git diff --name-only
```

Expected: inventory test and consistency check pass; the final diff lists only this task’s deliberate changes plus pre-existing unrelated edits.

- [ ] **Step 2: Report the final ownership boundary**

Report the location of the live script, skill, procedure, journal, inventory, and the unchanged archive project. Do not close FT-20260826-003 without the user’s explicit task-finish confirmation.
